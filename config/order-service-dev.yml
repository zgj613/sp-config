spring:
  application:
    name: order-service
    
#需要配合集群搭建,所以要注掉!
#server:
#  port: 8201

eureka:
  client:
    service-url:
      defaultZone:  http://eureka1:2001/eureka, http://eureka2:2002/eureka

#feign
feign:
  hystrix:
    enabled: true

#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
        
---
spring:
  profiles: order1
  
server:
  port: 8201
  
---
spring:
  profiles: order2
  
server:
  port: 8202